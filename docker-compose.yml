services:
  agendador-horarios:
    build:
      context: .
      dockerfile: Dockerfile
    image: agendador-horarios:1.0.0
    container_name: agendador-horarios
    ports:
      - "8080:8080"
    environment:
      - TZ=America/Sao_Paulo