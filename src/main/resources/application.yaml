spring:
  application:
    name: agendador-horarios

  datasource:
    url: jdbc:h2:mem:agendamentosdb
    driver-class-name: org.h2.Driver
    username: admin
    password: admin123

  h2:
    console:
      enabled: true
      path: /h2-console